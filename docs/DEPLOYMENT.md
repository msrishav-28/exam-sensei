# ExamSensei Deployment Guide

This guide takes a fresh repo to a working production deployment on the free tiers of:

| Layer | Service |
|---|---|
| Database + auth | Supabase |
| Backend API | Render (Python runtime, no Docker) |
| Frontend | Vercel |
| Exam-data refresh job | GitHub Actions cron |

End-to-end cost ceiling: **$0 until ~100 active users.** First spend trigger is usually the Gemini Flash daily quota, at which point you can either pin Groq (also free) via env var, or add `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` to upgrade with zero code change.

---

## 1. Supabase

### 1a. Create the project

1. Sign in at <https://supabase.com> → **New project**.
2. Region: pick the one closest to your users (Singapore = closest to India).
3. Save the database password somewhere safe.

### 1b. Capture the credentials

From **Project Settings → API**, copy:

| Name | Used by |
|---|---|
| `Project URL` | `SUPABASE_URL` env (backend) and your frontend Supabase client |
| `anon public` key | `NEXT_PUBLIC_SUPABASE_ANON_KEY` env (frontend) |
| `service_role` key | `SUPABASE_SERVICE_ROLE_KEY` env (GitHub Actions scraper only — never the frontend) |
| `JWT Secret` | `SUPABASE_JWT_SECRET` env (backend) |

From **Project Settings → Database**, copy the **Transaction pooler** connection string (port 6543; ends in `?sslmode=require`). That's your `DATABASE_URL` for Render.

### 1c. Install the auth-to-profile trigger

Open the SQL Editor in Supabase and run [`backend/migrations/001_supabase_profile_trigger.sql`](../backend/migrations/001_supabase_profile_trigger.sql) verbatim.

This installs two triggers on `auth.users`:
- **Insert** → creates the matching row in `public.users` (copying `email` + `raw_user_meta_data` for `name`, `education_level`, `state`, `category`, `budget`).
- **Update** → keeps `public.users.email` and `is_verified` in sync.

Re-running is safe (`CREATE OR REPLACE FUNCTION` + `DROP TRIGGER IF EXISTS`).

The `public.users` and other application tables themselves are created automatically by SQLAlchemy on the backend's first boot via `create_tables()` — you don't need to apply any other SQL.

---

## 2. Render — backend API

The repository ships a **Render Blueprint** at [`render.yaml`](../render.yaml). It uses Render's native Python runtime — **no Dockerfile, no docker-compose**.

### 2a. Deploy

1. Sign in to Render → **New → Blueprint**.
2. Pick this repo. Render reads `render.yaml` and proposes a single web service named `examsensei-api`.
3. Click **Apply**.

### 2b. Set the secrets

In the Render dashboard, open the service → **Environment** and set the variables Render couldn't auto-fill (marked `sync: false` in the blueprint):

| Variable | Value |
|---|---|
| `DATABASE_URL` | Supabase pooler connection string from step 1b |
| `SUPABASE_URL` | Supabase project URL |
| `SUPABASE_JWT_SECRET` | Supabase JWT secret |
| `ALLOWED_ORIGINS` | Comma-separated frontend origins, e.g. `https://exam-sensei.vercel.app,https://yourdomain.com` |
| `GOOGLE_API_KEY` | *(optional)* enables Gemini Flash for richer chat responses |
| `SENTRY_DSN` | *(optional)* error tracking |

`SECRET_KEY` is generated by Render automatically (not used by the new Supabase-Auth flow, but harmless to keep).

### 2c. First boot

Render runs:

```bash
pip install -r requirements.txt
uvicorn app_v2:app --host 0.0.0.0 --port $PORT
```

On startup the app calls `create_tables()` which provisions any missing tables in Supabase. The very first cold start takes ~30–60 s; subsequent requests are fast.

Healthcheck: `GET /api/v1/health` → `{"status": "healthy"}`. Render uses this to know when the service is up.

> **Free tier note:** Render free dynos sleep after 15 min of idleness. The first request after a sleep takes ~30 s. Upgrade to the $7/mo Starter plan to keep the dyno always-on.

---

## 3. Vercel — frontend

The frontend is a Next.js app under `frontend/`. The repository ships [`vercel.json`](../vercel.json) at the repo root configured for that subdirectory.

1. Sign in to Vercel → **Add New Project** → import this repo.
2. Vercel auto-detects Next.js. Confirm Framework Preset: **Next.js**, Root Directory: `/` (Vercel reads `vercel.json` to find the `frontend` subdirectory).
3. Set environment variables (Settings → Environment Variables):

| Variable | Value |
|---|---|
| `NEXT_PUBLIC_API_URL` | `https://examsensei-api.onrender.com/api/v1` (your Render service URL) |
| `NEXT_PUBLIC_SUPABASE_URL` | Same Supabase project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | The Supabase `anon public` key |

4. Click **Deploy**. Future pushes to `main` redeploy automatically.

> After the first frontend deploy completes, copy its URL back into Render's `ALLOWED_ORIGINS` env so CORS lets the browser through.

---

## 4. GitHub Actions — weekly exam-data refresh

The scraper does **not** run on the Render dyno. It runs in GitHub Actions so it doesn't burn through Render's 750 h/mo free quota.

In your GitHub repo → **Settings → Secrets and variables → Actions → New repository secret**, add:

| Secret | Value |
|---|---|
| `GOOGLE_API_KEY` | Same Gemini key as the backend uses |
| `SUPABASE_URL` | Same Supabase project URL |
| `SUPABASE_SERVICE_ROLE_KEY` | The `service_role` key (server-side only, never in frontend code) |

The workflow [`refresh_exam_data.yml`](../.github/workflows/refresh_exam_data.yml) is then ready:
- Scheduled: **Sundays 04:00 UTC** (≈ 09:30 IST).
- Manual: GitHub → Actions → *Refresh exam data* → **Run workflow**.

What the run does: read `backend/data/exam_sources.yaml`, fetch each exam's official page through Jina Reader (with Crawl4AI as fallback), pass the cleaned text to Gemini Flash for structured extraction, validate with Pydantic, and upsert into Supabase by `exam.code`. Topics for the exam are atomically replaced (delete + insert in the same transaction).

Logs appear in the GitHub Actions run output. Exit code is non-zero if more than half the sources failed, so failures are visible.

---

## 5. Smoke test the full stack

```bash
# Backend health
curl https://<your-render-service>.onrender.com/api/v1/health

# Browser flow:
# 1. Open https://<your-vercel-app>.vercel.app
# 2. Sign up via the Supabase auth UI.
# 3. After the trigger fires, verify in Supabase Table Editor that a matching
#    row appears in public.users.
# 4. From the dashboard, hit GET /api/v1/auth/me — should return the new user.
```

For a one-shot exam refresh: GitHub → Actions → Refresh exam data → Run workflow → check the Supabase `exams` table for fresh rows. Re-run to confirm idempotency (no duplicates).

---

## 6. Updating the deployment

| Change | What to do |
|---|---|
| Backend code change | Push to `main`. Render auto-deploys. |
| Frontend code change | Push to `main`. Vercel auto-deploys. |
| New env var added to backend | Add it in Render → Environment, then click **Manual Deploy → Deploy latest commit**. |
| Schema change (rare) | Update `backend/models.py`. The next backend boot calls `create_tables()` which adds *new* tables. For column-level changes, apply the ALTER manually in the Supabase SQL Editor. |
| New exam source | Append to `backend/data/exam_sources.yaml`, push, then trigger the GH Actions workflow manually. |

---

## 7. Cost monitoring

Set yourself a reminder to check these dashboards monthly:

| Service | What to check |
|---|---|
| Supabase | DB size (< 500 MB free), MAU (< 50 k free), egress (< 2 GB free) |
| Render | Dyno hours (< 750 h free) |
| Vercel | Build minutes, bandwidth (very generous on Hobby) |
| Google AI Studio | Gemini RPD (250) and RPM (10) |
| Groq Console | RPM (30) and daily request count |
| GitHub Actions | Minutes used (2000 free for private repos; unlimited for public) |

When any one hits ~80%, decide whether to upgrade that single service. Nothing in this stack is locked in — the LLM provider, in particular, is swap-by-env-var.
