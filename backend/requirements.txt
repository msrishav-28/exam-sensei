# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# Authentication: Supabase Auth issues HS256 JWTs that we verify locally
# with python-jose. No bcrypt/passlib needed — Supabase owns password hashing.
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1

# HTTP & API
httpx==0.28.1
requests==2.32.3

# Web scraping deps live in requirements-scraper.txt (used only by the
# GitHub Actions refresh pipeline; the API service doesn't carry them).

# Rate Limiting
redis==5.2.1
slowapi==0.1.9

# Email validation (used by EmailStr)
email-validator==2.2.0

# Monitoring (optional, only initialized when SENTRY_DSN is set)
sentry-sdk==2.19.2

# LLM providers (pluggable; backend picks the first one whose API key is set)
google-genai>=1.0.0
groq>=0.13.0
openai>=1.55.0
anthropic>=0.40.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0

# Utilities
python-dateutil==2.9.0.post0
