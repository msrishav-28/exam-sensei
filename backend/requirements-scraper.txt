# ExamSensei scraper — deps for the GitHub Actions refresh pipeline.
# Kept separate from the API requirements so the Render slug stays small.

crawl4ai>=0.4.0
markitdown[all]>=0.0.1a3
google-genai>=1.0.0
supabase>=2.7.4
pydantic>=2.10.4
pyyaml>=6.0
httpx>=0.28.1
